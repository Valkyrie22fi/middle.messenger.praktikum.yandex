<div class="chat__list">
  {{> ChatHeader photo="src/assets/acMain.png" title="Профиль" page="profile" alt="Фото профиля"}}
  {{> ChatSearch }}
  <ul class="chat__blocks">
    {{#each (blocks)}}
    {{> ChatListBlock }}
    {{/each}}
  </ul>
</div>
