<div class="chat__list">
    {{> ChatHeader photo="acMain.png" title="Профиль" page="profile"}}
    {{> ChatSearch }}
     <ul class="chat__blocks">
        {{#each (blocks)}}
            {{> ChatListBlock }}
        {{/each}}
    </ul>
</div>